#include <iostream> //Librerias
#include <conio.h>
using namespace std;

main()
{

//Ingreso Usuario

    persona userRegistrado;
    bool accesoUsuario;
    accesoUsuario = userRegistrado.loginPersona();
    if (accesoUsuario)
    {
        int opciones;
        bool repetir = true;
        do
        {

        }while (repetir);
    }

    getch();
	return 0;
}
